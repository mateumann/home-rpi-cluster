apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "postgresql"
spec:
  storageClassName: "manual"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "4Gi"
  volumeName: "nextcloud-postgresql"